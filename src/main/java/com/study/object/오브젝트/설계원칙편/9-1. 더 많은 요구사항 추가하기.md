---

# 핵심 요약 — 인터페이스 오염 문제와 ISP(Interface Segregation Principle)

## 1. 문제 상황 요약

Destroy / Throw 같은 **새 명령을 추가하면서**,
**Carrier 인터페이스 변경이 전혀 관련 없는 클래스들(WorldMap 등)까지 영향을 주는 문제 발생**

### 발생한 현상

* Destroy 기능 때문에 `Carrier.remove()` 시그니처 변경
* WorldMap, ForwardingCarrier 등 **Destroy와 무관한 클래스도 수정 필요**
* 요구사항 하나가 **시스템 전체 인터페이스 변경으로 확산됨**

👉 **변경 영향 범위가 통제되지 않음**

---

## 2. 근본 원인 — "뚱뚱한 인터페이스"

Carrier 인터페이스가 **서로 다른 클라이언트 요구를 모두 떠안음**

### 서로 다른 사용 패턴

| 클래스      | 사용하는 기능              |
| -------- | -------------------- |
| Game     | items 조회만 사용         |
| Transfer | find, add, remove 사용 |
| Destroy  | find, remove 사용      |
| WorldMap | add만 사용              |

👉 **하나의 인터페이스에 서로 다른 책임이 섞임**

---

## 3. 결과 — 설계적 문제

### (1) 변경 전파 폭발

* Destroy 요구 변경 → Carrier 변경 → WorldMap까지 수정

### (2) 불필요한 구현 강제

* 사용하지 않는 메서드를 **빈 구현으로 오버라이드**
* LSP(리스코프 치환 원칙) 위반 위험 증가

### (3) 결합도 증가

* 서로 무관한 기능들이 **인터페이스로 엮임**

---

## 4. 핵심 교훈

> **"인터페이스는 구현자가 아니라, 클라이언트를 기준으로 설계해야 한다."**

### 문제의 본질

* 인터페이스가 **기능 중심이 아니라 구조 중심으로 설계됨**
* 서로 다른 역할이 **한 타입에 강제로 묶임**

---

## 5. 해결 전략 — ISP (Interface Segregation Principle)

## 클라이언트별로 인터페이스 분리

### 예시 분리

```text
ItemsReadable   → items 조회
ItemFindable    → find
ItemAddable     → add
ItemRemovable   → remove
```

### Destroy 전용 인터페이스

```text
Destroyable → find + remove
```

### Transfer 전용 인터페이스

```text
Transferable → find + add + remove
```

👉 **클라이언트는 자신이 쓰는 기능만 의존**

---

## 6. 핵심 효과

### ✅ 변경 영향 최소화

Destroy 변경 → Destroy 인터페이스만 영향
WorldMap / Transfer 영향 없음

### ✅ 결합도 감소

* 서로 무관한 기능 간 의존 제거

### ✅ LSP / OCP 준수

* 빈 메서드 제거
* 타입 치환 안정성 확보

### ✅ 확장성 증가

* 새로운 명령 추가 시 **기존 코드 수정 최소화**

---

## 7. 핵심 메시지 (한 줄 요약)

> **"모든 클라이언트를 만족시키는 인터페이스를 만들려 하지 말고,
> 각 클라이언트에 맞는 인터페이스를 나눠라."**

---

## 8. 기억용 초압축 요약

```text
문제: Carrier 인터페이스가 Destroy 요구 때문에 오염됨
원인: 서로 다른 책임을 하나의 인터페이스에 몰아넣음
결과: 변경 전파, 결합도 증가, LSP 위반 위험
해결: 클라이언트별 인터페이스 분리 (ISP)
핵심: '인터페이스는 구현자가 아니라 사용자 기준'
```

---
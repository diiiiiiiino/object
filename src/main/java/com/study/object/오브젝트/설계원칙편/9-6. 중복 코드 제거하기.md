---

# 🎮 CLI · GUI 게임 통합 리팩터링 핵심 요약

## (DRY · DIP · ISP · GameLoop 추상화 · 확장 가능한 구조)

---

## 1️⃣ 문제 상황 — CLI / GUI 코드 중복과 결합 증가

### 클래스 분류

* **CLI 전용 클래스**
* **GUI 전용 클래스**
* **CLI·GUI 공통 클래스**

### 문제

* CLIGame / GUIGame 에 **중복 코드 다수 존재**
* world, CommandParser, run, play, executeCommand 로직 반복 구현
* 요구사항 변경 시 **중복 코드 전부 수정 필요** → 유지보수 비용 증가

> ❌ 중복 코드는 유지보수의 적

---

## 2️⃣ DRY 원칙 적용 — 중복 코드는 합성으로 제거

### 전략

* **상속보다 합성(Composition) 사용**
* CLI / GUI 공통 로직 → **Game 공통 클래스**로 이동
* CLIGame, GUIGame 은 **Game을 포함(합성)**하여 실행

### 결과 구조

* CLIGame / GUIGame → 실행 환경 담당
* Game → 공통 게임 로직 담당

> ✅ 중복 제거 + 책임 명확화

---

## 3️⃣ 새로운 문제 — CLI / GUI 실행 흐름 선택 문제

### 상황

Game 클래스가:

* CLIGame 의 play/stop 호출
* GUIGame 의 play/stop 호출

→ **구현체를 직접 선택해야 하는 문제 발생**

### 해결 방향

> **상위(Game)는 하위(CLI/GUI)에 의존하면 안 된다**

---

## 4️⃣ GameLoop 인터페이스 도입 (DIP 적용)

### 핵심 아이디어

* Game 은 **구현체(CLI/GUI)** 대신 **GameLoop 인터페이스**에 의존

### 역할 분리

| 역할       | 담당            |
| -------- | ------------- |
| Game     | 흐름 제어         |
| GameLoop | 실행 루프 정의      |
| CLIGame  | CLI 루프 구현     |
| GUIGame  | GUI 이벤트 루프 구현 |

### 효과

* Game 은 CLI / GUI 구분 없이 동일하게 동작
* **Dependency Inversion Principle (DIP)** 충족

---

## 5️⃣ 확장성 확보 — OCP 충족

### 구조적 이점

* 새로운 실행 환경 추가 시 (예: Web, Mobile, Server)
  → **GameLoop 구현체만 추가**
* Game 코드 수정 없음

> ✅ Open-Closed Principle 충족
> **확장에는 열려 있고, 수정에는 닫힘**

---

## 6️⃣ LSP 관점 — GUI input() 문제 발견

### 문제

* GUIGame 의 `input()` 메서드가 **아무 동작도 하지 않음**
* → **리스코프 치환 원칙(LSP) 위반 위험**

### 원인

* GUI 는 input() 기능이 필요 없음
* 그러나 인터페이스에 포함되어 **불필요 구현 발생**

---

## 7️⃣ ISP 적용 — Input / Output 인터페이스 분리

### 문제 요약

> 클라이언트가 **사용하지 않는 메서드에 의존**하고 있음

### 해결 — 인터페이스 분리

| 인터페이스           | 책임                   |
| --------------- | -------------------- |
| InputInterface  | 입력 전용 (CLI 전용)       |
| OutputInterface | 출력 전용 (CLI / GUI 공통) |

### 결과 의존성

* CLIGame → Input + Output
* GUIGame → Output 만 의존
* Console → Input + Output 구현
* GUI → Output 만 구현

> ✅ Interface Segregation Principle 충족

---

## 8️⃣ DIP + DI 적용 — 구현 교체 가능 구조

### 구조 특징

* Game / World → **추상 인터페이스(Input/Output)** 에만 의존
* 실제 구현(Console / GUI) 은 **Dependency Injection** 으로 주입

### 효과

* CLI ↔ GUI **코드 변경 없이 교체 가능**
* 테스트 시 Fake IO 주입 가능
* 외부 기술 변화에도 유연

---

## 9️⃣ 전체 구조의 설계 성과

### Before

* CLI / GUI 로직 중복
* Game 이 구현체 직접 의존
* 인터페이스에 불필요 메서드 포함
* 변경 시 영향 범위 큼

### After

* 공통 로직 통합
* 실행 환경 분리
* DIP / ISP / DRY / OCP 만족
* 수정 범위 최소화
* **확장 가능한 아키텍처 완성**

---

## 🔑 핵심 설계 원칙 정리

| 원칙  | 적용 내용                         |
| --- | ----------------------------- |
| DRY | CLI / GUI 중복 제거               |
| SRP | 실행 흐름 / 입출력 / 게임 로직 분리        |
| DIP | Game → GameLoop / IO 인터페이스 의존 |
| ISP | Input / Output 인터페이스 분리       |
| OCP | 새 환경 추가 시 기존 코드 수정 없음         |
| LSP | 불필요 구현 제거                     |

---

## 10️⃣ 핵심 교훈 (한 문장 요약)

> **“CLI와 GUI 실행 환경의 차이를 추상화하고, 실행 루프와 입출력 책임을 분리하여 중복 없이 확장 가능한 게임 구조로 리팩터링했다.”**

---
---

# 핵심 요약 — 인터페이스 분리 + 변경 이유 분리 설계

## 1. 문제의 본질 — “서로 다른 클라이언트 요구가 하나의 인터페이스에 섞임”

* `RecurringPlan` 인터페이스에 **DateInterval용 기능**과 **Schedule용 기능**이 함께 들어감
* 그 결과:

    * `MonthlyPlan`이 **필요 없는 apply 메서드까지 구현**
    * DateInterval 요구 변경이 **MonthlyPlan까지 수정**하게 만듦
* 즉, **클래스가 자신과 무관한 변경에 끌려다님** → 설계 취약

> ❗ 인터페이스 변경의 원인은 구현체가 아니라 **클라이언트 요구 변화**다

---

## 2. 문제 패턴 — 인터페이스 오염 (Interface Pollution)

### 증상

* 어떤 클래스는 `apply`만 필요
* 어떤 클래스는 `include / reschedule`만 필요
* 하지만 **모두 하나의 인터페이스를 강제로 구현**

### 결과

* 불필요한 메서드 구현
* 의미 없는 예외 / 빈 메서드
* 변경 전파 범위 확대
* 리스코프 치환 원칙(LSP) 위반 위험

---

## 3. 핵심 해결 전략 — **인터페이스 분리 원칙 (ISP)**

### 원칙

> 클라이언트가 **사용하지 않는 메서드에 의존하지 않게 하라**

### 적용 방식

#### 기존 (문제)

```
RecurringPlan
 ├─ apply (DateInterval 요구)
 ├─ include (Schedule 요구)
 └─ reschedule (Schedule 요구)
```

#### 개선 (분리)

```
TemporalFilterInterface   → DateInterval 전용
RecurringPlanInterface    → Schedule 전용
```

---

## 4. 역할 재정의 — 책임을 “변경 이유” 기준으로 분리

### DateInterval 관점

* 필요한 것은 날짜 필터링
* → `TemporalFilterInterface`

### Schedule 관점

* 필요한 것은 반복 일정 관리
* → `RecurringPlanInterface`

### 결과

* `WeeklyPlan` → 두 인터페이스 모두 구현
* `MonthlyPlan` → Schedule 인터페이스만 구현

> 💡 각 클래스는 **자신이 실제로 필요한 인터페이스만 의존**

---

## 5. 설계 개선 효과

### ✅ 변경 영향 최소화

* DateInterval 변경 → `MonthlyPlan` 수정 불필요
* Schedule 변경 → DateInterval 로직 영향 없음

### ✅ 단일 책임 원칙(SRP) 충족

* 인터페이스마다 **변경 이유가 하나**

### ✅ 리스코프 치환 원칙(LSP) 강화

* 더 이상 **불필요한 메서드 오버라이드 없음**
* 모든 구현체가 **의미 있는 계약을 충족**

### ✅ 개방-폐쇄 원칙(OCP) 강화

* 기존 코드 수정 없이 **새 구현 클래스 추가로 확장 가능**

### ✅ 의존성 역전(DIP) 자연스럽게 충족

* 상위 정책은 **구현이 아니라 인터페이스에 의존**

---

## 6. 핵심 인사이트 — “인터페이스는 구현이 아니라 클라이언트가 만든다”

> 인터페이스는
> ❌ 제공자가 설계하는 것이 아니라
> ✅ **클라이언트가 필요로 하는 행위만 기준으로 설계**해야 한다

### 인터페이스 변경 이유 = 클라이언트 변경 이유

→ **클라이언트별 인터페이스 분리**가 가장 안정적인 구조

---

## 7. 기억에 남는 한 줄 요약

> **서로 다른 이유로 변하는 기능을 하나의 인터페이스에 넣지 마라.
> 변경 이유별로 인터페이스를 나누면 설계는 자동으로 유연해진다.**

---
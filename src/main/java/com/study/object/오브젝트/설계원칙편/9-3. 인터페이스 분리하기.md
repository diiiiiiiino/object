---

# 🎯 핵심 요약 — 설계 원칙 & 테스트 어드벤처 게임 리팩터링

## 1️⃣ 문제의 본질

초기 설계는 다음 문제를 가지고 있었음:

* 구체 클래스(Player, Room 등)에 강하게 의존
* 상속으로 코드 재사용 → 변경에 취약
* 인터페이스가 너무 많은 책임을 가짐
* 일부 클라이언트 변경이 **무관한 클래스까지 수정** 유발
* LSP / ISP / DIP / OCP 위반
* 테스트 어려움, 예측 불가능한 동작

---

## 2️⃣ 핵심 원칙 정리

### ✅ 개방-폐쇄 원칙 (OCP)

> 기존 코드를 수정하지 않고 확장으로 기능 추가해야 한다

**문제**

* 새로운 기능(throw, destroy 등) 추가 시 기존 클래스 수정 필요

**해결**

* 새로운 클래스 추가로 기능 확장
* 변경 대신 **확장**

---

### ✅ 리스코프 치환 원칙 (LSP)

> 자식 클래스는 부모 클래스를 완전히 대체 가능해야 한다

**위반 사례**

* Room이 Player를 대체하지 못함
* 특정 서브타입에서 동작 실패 (NullPointerException)

**해결 방향**

* 클라이언트가 **구체 타입이 아닌 역할(인터페이스)**에 의존
* 모든 서브타입이 **동일 계약(행동 기대)**을 만족해야 함

---

### ✅ 인터페이스 분리 원칙 (ISP)

> 클라이언트는 사용하지 않는 메서드에 의존하면 안 된다

**문제**
Carrier 인터페이스가 너무 많은 책임 포함:

* items
* find
* add
* remove

각 클라이언트가 일부만 사용 → 변경 시 연쇄 영향 발생

**해결**
인터페이스 분리:

```text
Source  → find, remove
Target  → add
Carrier → items (공통 조회)
```

➡️ **클라이언트별 최소 인터페이스만 의존**

---

### ✅ 의존성 역전 원칙 (DIP)

> 상위 모듈은 구체 클래스가 아닌 추상화에 의존해야 한다

**변경 전**

```
Game → Player (구체)
```

**변경 후**

```
Game → Carrier (추상)
Transfer → Source / Target (추상)
```

➡️ 유연성 & 테스트 가능성 향상

---

### ✅ 단일 책임 원칙 (SRP)

> 하나의 클래스는 하나의 이유로만 변경되어야 한다

**리팩터링**

* Game → Transfer 로직 분리
* Destroy → 별도 책임
* Inventory → 아이템 관리 책임 전담

➡️ 테스트, 유지보수 쉬워짐

---

## 3️⃣ 상속 vs 합성 — 핵심 결론

### ❌ 상속으로 재사용 시 문제

* 중복 코드 증가
* 조합 폭발 (CSV + Redis + XML 등)
* 부모 변경 → 모든 자식 영향
* 강한 결합

### ✅ 합성(Composition)으로 전환

* Inventory를 Player/Room에 **포함**
* Forwarding / Delegation 패턴 사용
* 기능 단위 객체 조합

**결론**

> 코드 재사용 목적이라면 **상속보다 합성이 더 안전**

---

## 4️⃣ Transfer / Destroy 구조 개선 요약

### 🎒 Carrier 역할 분리

| 역할        | 인터페이스   |
| --------- | ------- |
| 아이템 제공자   | Source  |
| 아이템 받는 대상 | Target  |
| 목록 조회     | Carrier |

### 🔁 Transfer

* Source → Target 이동
* isPossible / perform 분리 (CQS 준수)

### 💥 Destroy

* Source에서만 아이템 제거
* Target에 의존하지 않음 → 영향 범위 최소화

---

## 5️⃣ WorldMap / Room 리팩터링 핵심

**문제**

* Carrier 전체 구현 → 불필요한 메서드 포함
* 일부 기능만 필요하지만 전체 계약 강제됨

**해결**

* WorldMap = Target 역할만 수행
* Source 기능 제거 → LSP 준수
* 불필요한 override 제거

---

## 6️⃣ 인터페이스 변경이 위험한 이유

### ⚠️ 인터페이스는 "구현자"가 아니라 **클라이언트 요구**로 변경됨

**문제 예시**
Destroy 요구 변경 →
Carrier.remove 시그니처 변경 →
WorldMap, Transfer, Forwarding 전부 수정 필요

### ✅ 해결 전략

> 클라이언트별 인터페이스 분리
> → 변경 이유를 하나로 제한

---

## 7️⃣ DateInterval / WeeklyPlan / MonthlyPlan 요약 (ISP 적용)

### 문제

* RecurringPlan 인터페이스에 서로 다른 클라이언트 요구 혼합
* DateInterval 변경 → MonthlyPlan까지 영향

### 해결

**인터페이스 분리**

| 목적    | 인터페이스          |
| ----- | -------------- |
| 날짜 필터 | TemporalFilter |
| 반복 일정 | RecurringPlan  |

➡️ **변경 이유 = 1개**

---

## 8️⃣ Forwarding & Delegation 패턴

### 목적

* 중복 코드 제거
* 상속 대신 책임 위임

**패턴**

* ForwardingCarrier → 요청 전달
* Inventory → 실제 로직 담당

➡️ **느슨한 결합 + 재사용성 증가**

---

## 9️⃣ 전체 설계가 도달한 상태

### ✅ 만족한 원칙

* OCP (확장 가능)
* LSP (서브타입 안전)
* ISP (클라이언트별 인터페이스)
* DIP (추상화 의존)
* SRP (책임 분리)
* CQS (명령/조회 분리)
* 합성 중심 구조

---

## 🔑 한 문장 요약

> **“클래스는 역할만 알고, 행동은 계약으로 보장하며, 재사용은 상속이 아니라 합성으로 한다.”**

---
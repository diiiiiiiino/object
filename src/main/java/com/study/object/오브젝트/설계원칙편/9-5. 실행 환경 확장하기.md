---

# 핵심 요약 — 설계 원칙으로 게임 구조 개선하기

## 1️⃣ 문제의 출발점 — 하나의 클래스가 너무 많은 책임을 가짐

* 초기 Game 클래스에 **모든 로직이 집중**
* `run()` 메서드가 너무 커지고 결합도가 높아짐
* 변경 하나가 **여러 클래스에 연쇄 영향**

**문제점**

* 낮은 응집도
* 높은 결합도
* 테스트 어려움
* 요구사항 변경에 취약

---

## 2️⃣ 해결 전략 ① — 큰 메서드를 작은 책임으로 분해

### 적용 원칙: **단일 추상화 수준(Single Level of Abstraction)**

* `run()`을 **역할별 메서드로 분리**
* 각 메서드는 **한 가지 일만 수행**
* 코드 가독성과 재사용성 개선

**효과**

* Game 클래스 이해 쉬워짐
* 테스트 가능성 증가

---

## 3️⃣ 해결 전략 ② — 값 객체(Value Object)로 개념 명확화

### 목적

* 숨겨진 개념 드러내기
* 중복 제거
* 의미 중심 모델링

**결과**

* 클래스 크기 감소
* 설계 의도 명확
* 유지보수 용이

---

## 4️⃣ 해결 전략 ③ — SRP (Single Responsibility Principle)

### 핵심 원칙

> **클래스는 변경 이유가 하나여야 한다**

### Game 클래스 책임 분리

* 게임 흐름 제어 → Game
* 월드 상태 관리 → World
* 명령 처리 → CommandParser
* 입출력 → InputOutput

**결과**

* 변경 영향 범위 축소
* 책임 경계 명확화

---

## 5️⃣ 문제 발견 — 인터페이스 오염과 불필요한 의존성

### 사례

* MonthlyPlan, WeeklyPlan, DateInterval 예제
* 서로 다른 클라이언트 요구가 **하나의 인터페이스에 섞임**
* 한 변경이 **관련 없는 클래스까지 수정 유발**

### 원인

* 서로 다른 역할의 메서드를 **같은 인터페이스에 포함**

---

## 6️⃣ 해결 전략 ④ — 인터페이스 분리 원칙 (ISP)

### 핵심 원칙

> **클라이언트는 자신이 사용하지 않는 메서드에 의존하지 않아야 한다**

### 적용 방식

* 하나의 큰 인터페이스 → **여러 개의 작은 인터페이스로 분리**
* 예:

    * Source 역할 인터페이스
    * Target 역할 인터페이스
    * Carrier 역할 인터페이스
    * TemporalFilter 인터페이스

**효과**

* 불필요한 수정 방지
* 역할 기반 의존성 명확화

---

## 7️⃣ 해결 전략 ⑤ — 역할(Role) 중심 설계

### 예: 게임 아이템 이동 로직 리팩토링

* Source: 아이템 제공자
* Target: 아이템 수신자
* Transfer: 이동 책임
* Destroy: 제거 책임

**결과**

* 객체가 **“무엇인가”** 가 아니라 **“무슨 역할을 하는가”** 중심으로 재구성
* 불필요한 메서드 오버라이딩 제거

---

## 8️⃣ 해결 전략 ⑥ — DIP (Dependency Inversion Principle)

### 핵심 원칙

> **고수준 정책은 저수준 구현이 아닌 추상화에 의존해야 한다**

### 적용

* Game → Console 직접 의존 ❌
* Game → InputOutput 인터페이스 의존 ✅

**효과**

* 구현(Console → GUI) 교체 가능
* 테스트 더 쉬워짐 (Fake I/O 가능)

---

## 9️⃣ GUI 전환 — CLI → Swing 구조 개선

### 핵심 변화

* 입력 처리: Scanner → 이벤트 기반
* 출력 처리: 콘솔 → JTextArea
* Game 로직 수정 없이 UI 교체 가능

### 구조

* GUIGame → ActionListener 구현
* CommandParser → 명령 처리 담당
* InputOutput → 출력 추상화 유지

**결과**

* UI 변경에도 Game 로직 안정적 유지
* 확장 가능 구조 확보

---

## 🔟 최종 아키텍처 상태 — 책임 분리 완료

### Game 클래스 책임

* 전체 게임 흐름 제어
* 명령 실행 조율

### World 클래스 책임

* 실제 게임 명령 수행

### Parser / IO / UI 책임

* 입력 해석
* 출력 처리
* UI 이벤트 관리

---

## 1️⃣1️⃣ 설계 원칙 적용으로 얻은 효과

### ✅ 변경 영향 최소화

### ✅ 클래스 책임 명확

### ✅ 테스트 가능성 증가

### ✅ UI/기술 교체 쉬움

### ✅ 확장성 & 유지보수성 향상

### ✅ 코드 이해 난이도 감소

---

## 1️⃣2️⃣ 핵심 교훈 요약 (한 줄 요약 모음)

* **클래스는 하나의 책임만 가져야 한다**
* **인터페이스는 클라이언트 기준으로 나눠야 한다**
* **역할(Role) 중심으로 객체를 모델링하라**
* **구현이 아닌 추상화에 의존하라**
* **변경 이유가 하나가 되도록 설계하라**
* **UI / I/O / 로직을 분리하라**
* **확장은 쉽게, 수정은 최소화하라**

---

## 1️⃣3️⃣ 한 문장으로 요약

> **이 리팩토링의 본질은 “변경에 강한 구조를 만들기 위해 책임·역할·의존성을 분리한 것”이다.**

---